---
layout: post
title: "2026년 프론트엔드, 상태 관리는 이렇게! Zustand + Immer로 보일러플레이트를 끝내자!"
date: 2026-01-14T15:01:29.545Z
categories: [frontend, tech]
tags: [react, javascript, nextjs, typescript, tailwindcss]
---

안녕하세요, 프론트엔드 개발 전문 블로거, 김프론트입니다!
오늘, 2026년 1월 14일, 여러분의 개발 생산성을 한 단계 끌어올릴 따끈따끈한 기술 트렌드를 가져왔습니다. 바로 **Zustand와 Immer의 환상적인 조합**에 대한 이야기입니다.

---



## 🚀 도입: 지겨운 상태 관리 보일러플레이트, 이제 그만!

프론트엔드 개발에서 상태 관리는 언제나 핵심적인 과제였습니다. 복잡한 애플리케이션일수록 상태의 흐름을 명확하게 파악하고, 예측 가능하며 효율적으로 업데이트하는 것이 중요하죠. 하지만 많은 상태 관리 라이브러리들이 때로는 과도한 보일러플레이트 코드와 복잡한 설정으로 개발자의 어깨를 무겁게 만들곤 합니다. 특히 불변성(immutability) 유지를 위한 깊은 복사(deep copy) 작업은 코드 가독성을 해치고 버그를 유발하는 주범이 되기도 합니다.

이러한 고통 속에서, 우리는 더 가볍고, 더 직관적이며, 더 강력한 상태 관리 솔루션을 갈망해왔습니다. 그리고 2026년, 그 해답 중 하나로 **Zustand와 Immer의 조합**이 뜨거운 주목을 받고 있습니다. 이 둘의 시너지는 어떻게 우리의 개발 경험을 혁신할 수 있을까요?

## ✨ 본문: Zustand와 Immer, 두 영웅의 만남

### 핵심 개념 설명: 가벼움과 불변성의 만남

**Zustand**는 React 생태계에서 가장 빠르게 성장하고 있는 상태 관리 라이브러리 중 하나입니다. "작고, 빠르고, 확장 가능하며, 고통 없는" 경험을 제공하는 것을 목표로 합니다. Context Provider 랩핑 없이 훅(Hook) 기반으로 스토어를 생성하고 소비할 수 있어 매우 간결하며, 번들 사이즈가 작고 러닝 커브가 낮다는 장점이 있습니다. Redux와 같은 복잡한 액션, 리듀서 패턴 없이도 강력한 상태 관리가 가능하죠.

**Immer**는 불변성을 유지하면서도 마치 가변(mutable) 객체를 다루듯이 상태를 업데이트할 수 있게 해주는 라이브러리입니다. 내부적으로 "draft" 개념을 사용하여 원본 상태를 직접 수정하는 것처럼 코드를 작성하면, Immer가 알아서 불변성을 지키는 새로운 상태를 생성해 반환해줍니다. 이 덕분에 복잡한 중첩 객체나 배열의 업데이트 로직이 놀랍도록 간결해집니다.

이 둘이 만나면, Zustand의 간결함 위에 Immer의 강력한 불변성 관리 편의성이 더해져, 개발자는 상태 관리 로직에 대한 고민을 최소화하고 비즈니스 로직에 집중할 수 있게 됩니다.

### 실제 코드 예시: 사용자 프로필 업데이트로 보는 마법

`npm install zustand immer` 명령어로 두 라이브러리를 설치한 후, 아래와 같이 스토어를 구성해볼 수 있습니다.

```typescript
// stores/userStore.ts
import { create } from 'zustand';
import { immer } from 'zustand/middleware/immer'; // Immer 미들웨어 임포트

// 사용자 프로필 데이터 타입 정의
interface UserProfile {
  name: string;
  email: string;
  address: {
    street: string;
    city: string;
    zip: string;
  };
  tags: string[];
}

// 스토어의 상태와 액션 타입 정의
interface UserState {
  profile: UserProfile;
  updateName: (name: string) => void;
  updateStreet: (street: string) => void;
  addTag: (tag: string) => void;
  removeTag: (tagToRemove: string) => void;
}

// Zustand 스토어 생성 (Immer 미들웨어 적용)
export const useUserStore = create<UserState>()(
  immer((set) => ({
    profile: {
      name: "김프론트",
      email: "front@example.com",
      address: {
        street: "테스트로 123",
        city: "서울",
        zip: "01234",
      },
      tags: ["개발자", "프론트엔드"],
    },
    // Immer를 사용하면 draft 상태를 직접 수정하는 것처럼 보이지만,
    // 실제로는 불변성을 유지한 새로운 상태가 생성됩니다.
    updateName: (name) =>
      set((state) => {
        state.profile.name = name;
      }),
    updateStreet: (street) =>
      set((state) => {
        state.profile.address.street = street;
      }),
    addTag: (tag) =>
      set((state) => {
        state.profile.tags.push(tag); // 배열에 직접 push!
      }),
    removeTag: (tagToRemove) =>
      set((state) => {
        state.profile.tags = state.profile.tags.filter(tag => tag !== tagToRemove);
        // filter는 새로운 배열을 반환하므로 Immer 없이도 불변성을 지키지만,
        // Immer 내부에서도 안전하게 처리됩니다.
      }),
  }))
);
```

이제 이 스토어를 React 컴포넌트에서 활용하는 예시를 보겠습니다.

```typescript
// components/UserProfileEditor.tsx
import React, { useState } from 'react';
import { useUserStore } from '../stores/userStore';

const UserProfileEditor: React.FC = () => {
  // Zustand 훅을 사용하여 상태와 액션 함수를 가져옵니다.
  const { profile, updateName, updateStreet, addTag, removeTag } = useUserStore();

  const [newName, setNewName] = useState(profile.name);
  const [newStreet, setNewStreet] = useState(profile.address.street);
  const [tagInput, setTagInput] = useState('');

  const handleNameChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setNewName(e.target.value);
  };

  const handleStreetChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setNewStreet(e.target.value);
  };

  const handleUpdateName = () => {
    updateName(newName);
  };

  const handleUpdateStreet = () => {
    updateStreet(newStreet);
  };

  const handleAddTag = (e: React.FormEvent) => {
    e.preventDefault();
    if (tagInput.trim() && !profile.tags.includes(tagInput.trim())) {
      addTag(tagInput.trim());
      setTagInput('');
    }
  };

  return (
    <div style={{ padding: '20px', border: '1px solid #ccc', borderRadius: '8px' }}>
      <h2>사용자 프로필 편집</h2>
      <p><strong>이름:</strong> {profile.name}</p>
      <p><strong>이메일:</strong> {profile.email}</p>
      <p><strong>주소:</strong> {profile.address.street}, {profile.address.city}, {profile.address.zip}</p>
      <p>
        <strong>태그:</strong> {profile.tags.map((tag) => (
          <span key={tag} style={{
            display: 'inline-block',
            backgroundColor: '#e0e0e0',
            borderRadius: '12px',
            padding: '4px 8px',
            margin: '2px',
            fontSize: '0.9em'
          }}>
            {tag}
            <button
              onClick={() => removeTag(tag)}
              style={{
                background: 'none',
                border: 'none',
                color: '#888',
                marginLeft: '5px',
                cursor: 'pointer'
              }}
            >
              x
            </button>
          </span>
        ))}
      </p>

      <hr style={{ margin: '20px 0' }} />

      <div>
        <h3>정보 업데이트</h3>
        <div style={{ marginBottom: '10px' }}>
          <input
            type="text"
            value={newName}
            onChange={handleNameChange}
            placeholder="새 이름"
            style={{ marginRight: '10px', padding: '8px' }}
          />
          <button onClick={handleUpdateName} style={{ padding: '8px 12px' }}>이름 변경</button>
        </div>
        <div style={{ marginBottom: '10px' }}>
          <input
            type="text"
            value={newStreet}
            onChange={handleStreetChange}
            placeholder="새 거리 주소"
            style={{ marginRight: '10px', padding: '8px' }}
          />
          <button onClick={handleUpdateStreet} style={{ padding: '8px 12px' }}>거리 변경</button>
        </div>
        <form onSubmit={handleAddTag} style={{ marginBottom: '10px' }}>
          <input
            type="text"
            value={tagInput}
            onChange={(e) => setTagInput(e.target.value)}
            placeholder="새 태그 추가"
            style={{ marginRight: '10px', padding: '8px' }}
          />
          <button type="submit" style={{ padding: '8px 12px' }}>태그 추가</button>
        </form>
      </div>
    </div>
  );
};

export default UserProfileEditor;
```

보시다시피, Immer 덕분에 `set` 함수 안에서 `state.profile.tags.push(tag)`와 같이 마치 직접 객체를 수정하는 것처럼 코드를 작성할 수 있습니다. 이는 불변성을 수동으로 지키기 위해 `...state.profile.tags, tag`와 같은 스프레드 연산자를 사용하거나, 복잡한 `Object.assign` 혹은 `structuredClone`을 사용하는 것보다 훨씬 직관적이고 오류 발생 가능성을 줄여줍니다.

### 실무 적용 팁

1.  **스토어 구조화 (Slice Pattern):** 애플리케이션이 커지면 단일 스토어가 비대해질 수 있습니다. Redux Toolkit의 slice 개념처럼, Zustand에서도 여러 개의 작은 스토어를 만들거나 하나의 스토어 내에서 관련 상태와 액션을 그룹화하는 방식으로 관리할 수 있습니다.
    ```typescript
    // stores/index.ts (예시)
    import { create } from 'zustand';
    import { immer } from 'zustand/middleware/immer';

    interface CounterSlice {
      count: number;
      increment: () => void;
    }

    interface AuthSlice {
      isLoggedIn: boolean;
      login: () => void;
    }

    // 여러 슬라이스를 통합하는 방법
    export const useBoundStore = create<CounterSlice & AuthSlice>()(
      immer((set) => ({
        // CounterSlice
        count: 0,
        increment: () => set((state) => { state.count++; }),

        // AuthSlice
        isLoggedIn: false,
        login: () => set((state) => { state.isLoggedIn = true; }),
      }))
    );
    ```
    또는 여러 파일을 사용하는 방식도 좋습니다.

2.  **TypeScript 완벽 활용:** 위에 예시처럼 인터페이스를 명확하게 정의하여 강력한 타입 추론과 자동 완성을 활용하세요. 이는 대규모 프로젝트에서 코드의 안정성과 유지보수성을 크게 향상시킵니다.

3.  **성능 최적화 (Selector 사용):** Zustand는 상태가 변경될 때 컴포넌트가 자동으로 리렌더링됩니다. 만약 스토어의 특정 부분만 필요하다면, 셀렉터(selector)를 사용하여 불필요한 리렌더링을 방지할 수 있습니다.
    ```typescript
    // 전체 profile 객체가 아닌 name만 변경될 때 리렌더링
    const userName = useUserStore((state) => state.profile.name);
    ```
    이렇게 하면 `profile.address`나 `profile.tags`가 변경될 때는 `userName`을 사용하는 컴포넌트가 리렌더링되지 않습니다.

4.  **비동기 처리:** Zustand는 비동기 액션도 매우 쉽게 처리할 수 있습니다. `set` 함수를 비동기 함수 내에서 호출하면 됩니다.
    ```typescript
    interface UserState {
      // ...
      fetchUserProfile: () => Promise<void>;
    }

    export const useUserStore = create<UserState>()(
      immer((set) => ({
        // ...
        fetchUserProfile: async () => {
          // 로딩 상태 설정
          // set((state) => { state.isLoading = true; });

          const response = await fetch('/api/user');
          const data = await response.json();

          set((state) => {
            state.profile = data;
            // state.isLoading = false;
          });
        },
      }))
    );
    ```

5.  **디버깅:** Zustand는 Redux DevTools Extension과 호환됩니다. 스토어 생성 시 미들웨어로 연결하면 시간 여행 디버깅 등 강력한 기능을 활용할 수 있습니다.
    ```typescript
    import { create } from 'zustand';
    import { devtools, persist } from 'zustand/middleware';
    import { immer } from 'zustand/middleware/immer';

    export const useMyStore = create<MyState>()(
      devtools( // DevTools 미들웨어
        persist( // 로컬 스토리지에 저장 (선택 사항)
          immer((set) => ({
            // ...
          })),
          { name: 'my-app-storage' } // persist 설정
        )
      )
    );
    ```

## 🎯 마무리: 간결함 속의 강력함

Zustand와 Immer의 조합은 프론트엔드 상태 관리의 새로운 지평을 열고 있습니다. Zustand의 뛰어난 간결함과 직관적인 API는 러닝 커브를 대폭 낮추고, Immer의 마법 같은 불변성 관리는 복잡한 상태 업데이트 로직을 단순화하여 개발자의 생산성을 극대화합니다. 더 이상 보일러플레이트에 시간을 낭비하지 않고, 핵심 비즈니스 로직에 집중하며 더 나은 사용자 경험을 만들어낼 수 있게 된 것이죠.

아직 이 조합을 사용해보지 않으셨다면, 오늘 당장 여러분의 프로젝트에 적용해보시길 강력히 추천합니다. 한 번 경험하면 다시는 이전으로 돌아가고 싶지 않을 만큼 매력적인 개발 경험을 선사할 것입니다. 다음번에는 Zustand의 비동기 처리와 테스트 전략에 대해 더 깊이 다뤄보겠습니다. 기대해주세요!

---

## ## 참고 자료

- [Stop Uploading Your Files: How I Built a 100% Client-Side Image Converter with React & Vite](https://dev.to/amineace/stop-uploading-your-files-how-i-built-a-100-client-side-image-converter-with-react-vite-4n3j) by Amine Ace
- [🔥 Stop Writing Boilerplate: Zustand + Immer, Done Once and Right](https://dev.to/abdoseadaa/stop-writing-boilerplate-zustand-immer-done-once-and-right-33cd) by abdelrahman seada
- [Top 5 React Animation Libraries: Bring Life to Your Web Applications](https://dev.to/riteshkokam/top-5-react-animation-libraries-bring-life-to-your-web-applications-2hm8) by Ritesh Kokam
- [Building a Multi-Counter App in React — Improving Logic Step by Step](https://dev.to/abhishek_arankal_4ac78c11/building-a-multi-counter-app-in-react-improving-logic-step-by-step-53bf) by Abhishek Arankal
- [Building Reactive Status Indicators with the Mantine LED Component](https://dev.to/undolog/building-reactive-status-indicators-with-the-mantine-led-component-4d6h) by Giovambattista Fazioli